# NEXT_PUBLIC_GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
NEXT_PUBLIC_GEMINI_API_KEY="MY_GEMINI_API_KEY"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# NEXT_PUBLIC_LOG_LEVEL: Set to 'info', 'warn', 'error', or 'debug' (default: 'info')
NEXT_PUBLIC_LOG_LEVEL="info"

# NEXT_PUBLIC_COST_WARNING_THRESHOLD: Threshold in USD to trigger a warning in the cost tracker
NEXT_PUBLIC_COST_WARNING_THRESHOLD="1.00"
